name: Question Bank Blueprint
on:
  push:
    branches:
      - feature/central-question-bank-blueprint
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --test question-bank-blueprint-contract.test.mjs
