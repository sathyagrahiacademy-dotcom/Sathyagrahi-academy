name: Exam Scope Service Role Grants
on:
  push:
    branches:
      - fix/exam-scope-syllabus-service-role-grants
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --test exam-scope-service-role-grants.test.mjs
